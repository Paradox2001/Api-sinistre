apiVersion: v1
kind: Service
metadata:
  name: axasinistre
  labels:
    app: axasinistre
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31000
  selector:
    app: axa_sinistre
